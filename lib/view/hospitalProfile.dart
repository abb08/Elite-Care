import 'package:flutter/material.dart';

///todo: logo
///name + rating
///book now =>to docs section
///sections
///about the center=> prief+locations+ specializations
///docs=> filters by specialization + location+insurance 
///ratings
///insurance companies

class HospitalProfile extends StatelessWidget {
  const HospitalProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
